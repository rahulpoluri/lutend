{
  "name": "@lutend/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Lutend React Native mobile app for iOS and Android",
  "main": "index.js",
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "test": "echo 'No tests configured yet for mobile app'",
    "lint": "echo 'No linting configured yet for mobile app'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@lutend/types": "workspace:*",
    "@lutend/api-client": "workspace:*",
    "@lutend/design-tokens": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-native": "^0.73.0",
    "typescript": "^5.3.3"
  }
}
