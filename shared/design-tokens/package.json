{
  "name": "@lutend/design-tokens",
  "version": "1.0.0",
  "description": "Shared design tokens (colors, typography, spacing) from Figma",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'No tests configured for design-tokens (static data)'",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "files": [
    "dist"
  ]
}
